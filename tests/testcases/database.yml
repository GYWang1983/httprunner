config:
    name: "check database"
    variables:
        uid: 9001
        device_sn: "TESTCASE_CREATE_XXX"

teststeps:
-
    name: check database
    database:
      dialect: mysql
      connection:
         url: localhost/testrunner
         user: root
      sql: SELECT * FROM `case`;
    validate:
        - eq: ["count", 4]
    extract:
        - count: count
        - name: first.name
#    export:
#        - table: [user]
        #- sql: "SELECT 1"
        #- script: sql/export.sql

