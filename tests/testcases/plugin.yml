config:
    name: "check ping plugin"
    variables:
        uid: 9001
        device_sn: "TESTCASE_CREATE_XXX"

teststeps:
-
    name: check ping
    plugin:
      script: tcpping
      url: baidu.com
      timeout: 5
    validate:
        - lt: ["result.time", 3]
-
    name: check ping2
    plugin:
      script: tcpping
      url: 127.0.0.1
      timeout: 5
#    extract:
#        - time: result.time
    validate:
        - lt: ["result.time", 3]

